{
  "name": "online-optimizer-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port ${FRONTEND_PORT:-5418}",
    "start": "vite --host 127.0.0.1 --port ${FRONTEND_PORT:-5418}",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port ${FRONTEND_PORT:-5418}"
  },
  "dependencies": {
    "vue": "^3.5.17"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^5.4.19"
  }
}
