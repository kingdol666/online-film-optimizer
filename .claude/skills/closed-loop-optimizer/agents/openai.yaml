interface:
  display_name: "Closed-Loop Optimizer"
  short_description: "Coordinate a native Claude Code AgentTeam for film-line optimization"
  brand_color: "#1D4ED8"
  default_prompt: "Use $closed-loop-optimizer to start a native Claude Code AgentTeam that coordinates quality, R&D, and process roles toward the user's optimization goal."
policy:
  allow_implicit_invocation: true
